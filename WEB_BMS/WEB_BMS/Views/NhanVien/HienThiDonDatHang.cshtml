
@{
    ViewBag.Title = "HienThiDonDatHang";
    Layout = "~/Views/Shared/Layout_NhanVien.cshtml";
}

@model IEnumerable<WEB_BMS.Models.DonNhapHang>


<link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet" />
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
<style>
    body {
        overflow-x: hidden;
        color: black;
        font-size: 16px;
    }

    a {
        text-decoration: none;
        color: black;
    }

    .sidebar {
        color: black;
        height: 100vh;
        position: fixed;
        top: 0;
        left: 0;
        z-index: 100;
        background-color: #f8f9fa;
        padding: 20px;
        margin-top: 100px;
    }

    .nav-link {
        color: black;
    }

    .text-primary {
        color: black !important;
        margin-top: 30px;
    }

    .text-secondary {
        margin-top: 20px;
    }

    .content {
        display: flex;
        gap: 20px;
        flex-wrap: wrap;
    }

    .table {
        width: 1000px;
    }

    .submenu {
        display: none;
    }
</style>
<div class="sidebar">
    <div class="text-primary"><a href="~/NhanVien/HienThiDonDatHang">Quản lý nhập hàng</a></div>
    <div id="toggle-submenu" class="text-primary" style="cursor: pointer;"><a href="~/NhanVien/HienThiQuanLyDonNhanHang">Nhập hàng</a></div>
    <hr style="margin-top" style="margin-top:30px" />
    <div class="text-primary" style="margin-top:10px">Khác</div>
</div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

<table class="table table-hover" width="100%">
    <thead class="thead-dark">
        <tr>
            <th>Mã đơn hàng</th>
            <th>Ngày nhập</th>
            <th>Nhân viên nhập</th>
            <th>Thành tiền</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(i => item.MaDonNhapHang)</td>
                <td>@Html.DisplayFor(i => item.NgayNhap)</td>
                <td>@Html.DisplayFor(i => item.MaNV)</td>
                <td>0d</td>
                <td>
                    <a href="~/NhanVien/ChiTietDonNH/@item.MaDonNhapHang">
                        <button type="button" class="btn btn-outline-dark">Xem</button>
                    </a>
                    <a href="~/NhanVien/NhapHang/@item.MaDonNhapHang">
                        <button type="button" class="btn btn-primary">Nhập hàng</button>
                    </a>
                    <button type="button" class="btn btn-danger" onclick="confirmDelete('@item.MaDonNhapHang')">Xóa</button>
                </td>
            </tr>
        }
    </tbody>
</table>

<script>
    function confirmDelete(maDonNhapHang) {
        if (confirm("Bạn có chắc chắn muốn xóa đơn hàng này?")) {
            
        }
    }
</script>
